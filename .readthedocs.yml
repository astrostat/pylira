version: 2

build:
  image: latest
  apt_packages:
    - r-base-dev
    - r-base
    - r-mathlib

python:
  version: 3.7
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs
        - all

formats: []
